{
	"role":{
		"name": "osr.sec.lti.backend.dac::read_write_g#",
		"schema_privileges":[{  "privileges_with_grant_option":[ "SELECT", "SELECT CDS METADATA", "EXECUTE","INSERT","UPDATE", "DELETE", "CREATE TEMPORARY TABLE" ]}]
    }
}
