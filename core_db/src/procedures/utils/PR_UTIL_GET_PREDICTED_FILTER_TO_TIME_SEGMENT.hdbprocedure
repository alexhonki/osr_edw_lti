PROCEDURE "procedures.utils::PR_UTIL_GET_PREDICTED_FILTER_TO_TIME_SEGMENT" (OUT O_TIME_SEGMENT varchar(6)
	) 
	LANGUAGE SQLSCRIPT
	SQL SECURITY INVOKER 
	--DEFAULT SCHEMA <default_schema_name>
	READS SQL DATA AS
BEGIN
	
	-- Get System Generated Date
	call "procedures.utils::PR_CONF_GET_PREDICTION_TIME_SEGMENT"(O_TIME_SEGMENT);
END;
